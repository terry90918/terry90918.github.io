# Regular Chrome mobile visual acceptance

- **Surface:** Chrome at a `375 × 812` viewport, served from the built static export at
  `http://localhost:3002/about`.
- **Light theme:** the 112px portrait and GitHub contribution chart visibly rendered; chart labels
  and contribution cells were readable, the four Connect links were visible, and the page had no
  horizontal overflow.
- **Dark theme:** after toggling the existing theme control, the portrait and chart visibly
  rendered again; the chart remained readable and all four contact links remained visible.
- This regular-browser check closes the mobile image-painting observation recorded during the
  earlier in-app-browser inspection.
